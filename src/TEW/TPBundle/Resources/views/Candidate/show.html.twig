{% extends '::base.html.twig' %}
{% block styles %}
     <link rel="stylesheet" type="text/css" href="{{ asset('bundles/tewtp/css/jquery.tagsinput.css') }}" />
    {# jQuery zoom on pictures #}
    <link rel="stylesheet" type="text/css" href="{{ asset('bundles/tewtp/css/bootstrap-magnify.css') }}" />
{% endblock styles %}

{% block scripts %}
    {# jQuery zoom on pictures #}
    <script src="{{ asset('bundles/tewtp/js/bootstrap-magnify.js') }}"></script>
    <script>
        $(function () {
            $('[data-toggle="popover"]').popover()
        })
    </script>
    {{ include('TEWTPBundle:Candidate:common/cdtedetails_request_script.js.twig') }}
    {{ include('TEWTPBundle:Candidate:common/cdte_add_comment_script.js.twig') }}
    {% if is_granted("ROLE_TEW_STAFF") %}
        {{ include('TEWTPBundle:Candidate:common/cdte_notes_script.js.twig') }}
    {% endif %}
{% endblock scripts %}

{% block body -%}
    {{ include('TEWTPBundle:Candidate:view.html.twig', {'entity': entity} ) }}
    {{ include('TEWTPBundle:Candidate:common/cdtedetails_request.html.twig', {'entity': entity} ) }}
{% endblock body %}